apiVersion: v1
kind: Service
metadata:
  name: nodejs-server-svc
  namespace: nginx-ingress
spec:
  selector:
    app: nodejs-server
  #type: NodePort
  type: ClusterIP
  ports:
    - port: 3000
      protocol: TCP
      #nodePort: 31000
